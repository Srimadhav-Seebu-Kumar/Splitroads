{
  "name": "@splitroads/db",
  "version": "0.1.0",
  "description": "Database migrations, schema helpers, and Drizzle ORM definitions for Split-Roads",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "migrate": "node dist/migrate.js",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "postgres": "^3.4.5",
    "drizzle-orm": "^0.38.3"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "drizzle-kit": "^0.30.1",
    "@types/node": "^22.10.2"
  }
}
